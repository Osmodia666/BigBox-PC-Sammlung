# Lokal erzeugte Laufzeit-/Build-Dateien - nicht Teil des Quellcodes
column_widths.cfg
settings.ini
lastfile.ini
spiele.csv
source/lib/
source/*.o
source/*.ppu
source/*.compiled
backup/
*.bak~

# Die fertige SpieleSammlung.zip im Root wird bewusst NICHT ignoriert -
# die wird manuell gebaut, gepackt und eingecheckt (siehe README). Die
# rohe .exe wird nicht eingecheckt (47 MB, ueber GitHubs 25-MB-Grenze
# fuer den Web-Upload).
*.exe
